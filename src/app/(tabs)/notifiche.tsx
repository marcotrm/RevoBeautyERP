/**
 * Tab Notifiche — qui andranno le notifiche push del centro.
 */
import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function NotificheScreen() {
  return (
    <PlaceholderScreen
      title="Notifiche"
      description="Qui troverai le notifiche e le novità del centro."
    />
  );
}
